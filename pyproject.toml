[project]
name = "piping-tag-extractor-worker"
version = "1.0.0"
description = "P&ID piping tag extraction API using Gemini vision on Cloudflare Python Workers"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.141,<1",
]

[dependency-groups]
dev = [
    "workers-py",
    "workers-runtime-sdk",
]
